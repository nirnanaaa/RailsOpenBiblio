.row
  .pull-right
    = link_to t('genre.new').capitalize, new_genres_path(), :class => 'btn'
.row.bit-center
  %table.table-hover.table-bordered.hundret.table-striped
    %thead
      %tr
        %td Name
        %td Description
         
    %tbody
    - Genre.find(:all).each do |genre|
      %tr.clickable
        %td= genre.name
        %td= truncate(genre.description,:length => 220)
