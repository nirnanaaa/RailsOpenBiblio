!!!
%html{:lang => "en"}
	%head
		%title= RPO
		
		
		%meta{:charset => "utf-8"}
		%meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
		%meta{:name => "author", :content => "author"}
		
		= stylesheet_link_tag  "application", :media => "all"

		= csrf_meta_tags
	%body.linear-gradient-background.dimmed
		#wrapper.container
			.row
				.logo
					= image_tag "layout/logo_top.png"
			%nav.row.navbar
				%nav.navbar-inner
					%ul.nav.pull-left
						%li
							= link_to 'Home', root_path
						%li.divider-vertical
						%li
							= link_to 'Library', '/library'
							
					%ul.nav.pull-right
						%form.navbar-search.pull-right{:method=>'GET', :action=>'/search'}
							%input.search-query.search{:type=>'text',:placeholder=>"Search",:name=>"keyword"}
			
			- if flash_messages.nil?
				.row.warning.alert.alert-block.alert-info
					%button.close{:type=>'button', 'data-dismiss'=>'alert'} &times;
					= raw flash_messages
				
			.row
				= yield
	
		= javascript_include_tag "application"
		/[if lt IE 9]
			%script(src="//html5shim.googlecode.com/svn/trunk/html5.js")
		%script
			$('.search').typeahead()
